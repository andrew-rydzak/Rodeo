import { React } from "react";

const NotFoundScreen = () => {
  return (
    <main style={{ margin: "0", padding: "0", minWidth: "250px" }}>
      <section
        style={{ width: "100%", textAlign: "center", marginTop: "100px" }}
      >
        <h2>Woops, no matching page found.</h2>
      </section>
    </main>
  );
};

export default NotFoundScreen;
